# Optional environment overrides; .env is NOT loaded automatically.
# Use your shell's normal environment configuration. No API keys belong here.
# BRIDGE_HOME=/home/grok/.grokbot-openai-bridge
# BRIDGE_CODEX_HOME=/home/grok/.codex
# BRIDGE_CODEX_BIN=/absolute/path/to/a/native/codex-executable
# Leave BRIDGE_CODEX_BIN unset to use the pinned official client dependency.
# ChatGPT URL, title, repository and branch belong in the project JSON example.
